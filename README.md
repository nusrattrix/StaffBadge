# StaffBadge (Vencord Plugin)

Adds simple **Owner/Admin/Mod** badges next to usernames in Discord, similar to the StaffTag plugin.

## 🔧 Installation
1. Open your Vencord `plugins` folder.
   ```sh
   cd ~/.config/Vencord/plugins
